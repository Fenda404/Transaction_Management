    <Window x:Class="Transaction_Management.Views.MainViews.MainLoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Transaction_Management.ViewModels"
        xmlns:uc="clr-namespace:Transaction_Management.Views.UserControls"
        mc:Ignorable="d"
        Title="Xác thực"
        Height="650"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="Black">

    <!-- Liên kết với MainLoginViewModel để xử lý logic chuyển trang -->
    <Window.DataContext>
        <vm:MainLoginViewModel />
    </Window.DataContext>

    <!-- Khai báo DataTemplate để Window biết phải gọi UI nào khi ViewModel thay đổi -->
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <uc:Login_UC />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:RegisterViewModel}">
            <uc:Register_UC />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <!-- ẢNH NỀN TRÀN VIỀN -->
        <Image Source="/Resources/Images/Backgrounds/hinhnen.png" 
               Stretch="UniformToFill">
            <Image.Effect>
                <BlurEffect Radius="10" />
            </Image.Effect>
        </Image>

        <!-- Lớp phủ màu đen trong suốt để làm dịu ảnh nền, tôn Form trắng lên -->
        <Rectangle Fill="Black" Opacity="0.15" />

        <!-- KHUNG CHỨA FORM ĐỘNG -->
        <!-- Content sẽ tự động hiển thị Login_UC hoặc Register_UC -->
        <ContentControl Content="{Binding CurrentView}" 
                        HorizontalAlignment="Stretch" 
                        VerticalAlignment="Stretch" />
    </Grid>
</Window>