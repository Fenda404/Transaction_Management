<UserControl x:Class="Transaction_Management.Views.UserControls.Settings_UC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">

    <UserControl.Resources>
        <!-- Style cho tiêu đề nhóm -->
        <Style x:Key="SettingHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="#2E7D32" />
            <Setter Property="Margin" Value="0,15,0,10" />
        </Style>

        <!-- Style cho Card bao quanh nhóm cài đặt -->
        <Style x:Key="SettingCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="CornerRadius" Value="15" />
            <Setter Property="Padding" Value="20" />
            <Setter Property="Margin" Value="0,0,0,20" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.05" Color="Black" />
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style cho dòng cài đặt lẻ -->
        <Style x:Key="SettingItemStyle" TargetType="Grid">
            <Setter Property="Margin" Value="0,8" />
        </Style>

        <!-- Style cho nút hành động nhỏ -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="#4CAF50" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#2E7D32" />
            <Setter Property="Padding" Value="15,5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E8F5E9" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="30,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!-- Header chính -->
                <RowDefinition Height="Auto" />
                <!-- User Info -->
                <RowDefinition Height="Auto" />
                <!-- Currency -->
                <RowDefinition Height="Auto" />
                <!-- Appearance -->
                <RowDefinition Height="Auto" />
                <!-- Notifications -->
                <RowDefinition Height="Auto" />
                <!-- Security -->
            </Grid.RowDefinitions>

            <!-- TIÊU ĐỀ TRANG -->
            <StackPanel Grid.Row="0" Margin="0,0,0,25">
                <TextBlock Text="CÀI ĐẶT HỆ THỐNG" FontSize="28" FontWeight="Bold" Foreground="#2E7D32" />
                <TextBlock Text="Tùy chỉnh trải nghiệm cá nhân của bạn" Foreground="#757575" FontSize="14" />
            </StackPanel>

            <!-- 1. THÔNG TIN NGƯỜI DÙNG -->
            <StackPanel Grid.Row="1">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Foreground="#2E7D32" FontSize="20" Margin="0,0,10,0"/>
                    <TextBlock Text="1. Thông tin người dùng" Style="{StaticResource SettingHeaderStyle}"/>
                </StackPanel>
                <Border Style="{StaticResource SettingCardStyle}">
                    <StackPanel>
                        <Grid Style="{StaticResource SettingItemStyle}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Tên người dùng" FontWeight="SemiBold" FontSize="15"/>
                                <TextBlock Text="{Binding Username}" Foreground="#757575" FontSize="13"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="Đổi Username" Style="{StaticResource ActionButtonStyle}" Command="{Binding ChangeUsernameCommand}"/>
                        </Grid>
                        <Separator Background="#F0F0F0" Margin="0,10"/>
                        <Grid Style="{StaticResource SettingItemStyle}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Mật khẩu" FontWeight="SemiBold" FontSize="15"/>
                                <TextBlock Text="••••••••••••" Foreground="#757575" FontSize="13"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="Đổi mật khẩu" Style="{StaticResource ActionButtonStyle}" Command="{Binding ChangePasswordCommand}"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- 2. TIỀN TỆ -->
            <StackPanel Grid.Row="2">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE8A1;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Foreground="#2E7D32" FontSize="20" Margin="0,0,10,0"/>
                    <TextBlock Text="2. Tiền tệ" Style="{StaticResource SettingHeaderStyle}"/>
                </StackPanel>
                <Border Style="{StaticResource SettingCardStyle}">
                    <StackPanel>
                        <Grid Style="{StaticResource SettingItemStyle}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="150" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Loại tiền tệ sử dụng" VerticalAlignment="Center" FontSize="15"/>
                            <ComboBox Grid.Column="1" ItemsSource="{Binding Currencies}" SelectedItem="{Binding SelectedCurrency}" Height="35" VerticalContentAlignment="Center"/>
                        </Grid>
                        <Separator Background="#F0F0F0" Margin="0,10"/>
                        <Grid Style="{StaticResource SettingItemStyle}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="150" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Định dạng hiển thị" VerticalAlignment="Center" FontSize="15"/>
                            <ComboBox Grid.Column="1" ItemsSource="{Binding Formats}" SelectedItem="{Binding SelectedFormat}" Height="35" VerticalContentAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- 3. GIAO DIỆN -->
            <StackPanel Grid.Row="3">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE790;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Foreground="#2E7D32" FontSize="20" Margin="0,0,10,0"/>
                    <TextBlock Text="3. Giao diện" Style="{StaticResource SettingHeaderStyle}"/>
                </StackPanel>
                <Border Style="{StaticResource SettingCardStyle}">
                    <StackPanel>
                        <Grid Style="{StaticResource SettingItemStyle}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Chế độ tối (Dark Mode)" VerticalAlignment="Center" FontSize="15"/>
                            <CheckBox Grid.Column="1" IsChecked="{Binding IsDarkMode}" VerticalAlignment="Center" />
                        </Grid>
                        <Separator Background="#F0F0F0" Margin="0,10"/>
                        <Grid Style="{StaticResource SettingItemStyle}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="200" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Màu chủ đạo" VerticalAlignment="Center" FontSize="15"/>
                            <ComboBox Grid.Column="1" ItemsSource="{Binding ThemeColors}" SelectedItem="{Binding SelectedThemeColor}" Height="35" VerticalContentAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- 4. THÔNG BÁO -->
            <StackPanel Grid.Row="4">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xEA8F;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Foreground="#2E7D32" FontSize="20" Margin="0,0,10,0"/>
                    <TextBlock Text="4. Thông báo" Style="{StaticResource SettingHeaderStyle}"/>
                </StackPanel>
                <Border Style="{StaticResource SettingCardStyle}">
                    <StackPanel>
                        <Grid Style="{StaticResource SettingItemStyle}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Cảnh báo khi vượt ngân sách" VerticalAlignment="Center" FontSize="15"/>
                            <CheckBox Grid.Column="1" IsChecked="{Binding BudgetAlert}" VerticalAlignment="Center" />
                        </Grid>
                        <Separator Background="#F0F0F0" Margin="0,10"/>
                        <Grid Style="{StaticResource SettingItemStyle}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Nhắc nhập chi tiêu hàng ngày" VerticalAlignment="Center" FontSize="15"/>
                            <CheckBox Grid.Column="1" IsChecked="{Binding DailyReminder}" VerticalAlignment="Center" />
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- 5. BẢO MẬT & HỆ THỐNG -->
            <StackPanel Grid.Row="5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE72E;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Foreground="#2E7D32" FontSize="20" Margin="0,0,10,0"/>
                    <TextBlock Text="5. Bảo mật" Style="{StaticResource SettingHeaderStyle}"/>
                </StackPanel>
                <Border Style="{StaticResource SettingCardStyle}">
                    <StackPanel>
                        <Button Content="Đăng xuất khỏi thiết bị này" 
                                Command="{Binding DataContext.SignOutCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                Background="#FFF1F0" Foreground="#D32F2F" BorderBrush="#FFCCC7" BorderThickness="1"
                                Height="45" FontWeight="Bold" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <TextBlock Text="&#xE8FB;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>