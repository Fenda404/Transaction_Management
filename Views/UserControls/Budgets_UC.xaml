<UserControl x:Class="Transaction_Management.Views.UserControls.Budgets_UC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Transaction_Management.Converters"
             xmlns:vm="clr-namespace:Transaction_Management.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="1000">

    <UserControl.DataContext>
        <vm:BudgetsViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <local:GreaterThanConverter x:Key="GreaterThanConverter" />
        <local:PercentToWidthConverter x:Key="PercentToWidthConverter" />

        <Style x:Key="BudgetCardStyle"
               TargetType="Border">
            <Setter Property="CornerRadius"
                    Value="15" />
            <Setter Property="Background"
                    Value="White" />
            <Setter Property="Padding"
                    Value="20" />
            <Setter Property="Margin"
                    Value="10" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15"
                                      ShadowDepth="2"
                                      Opacity="0.05"
                                      Color="Black" />
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Margin="10,0,0,25">
            <TextBlock Text="Budget Management"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#2E7D32" />
            <TextBlock Text="Set and track monthly spending limits"
                       Foreground="#757575"
                       FontSize="14" />
        </StackPanel>

        <Border Grid.Row="1"
                Style="{StaticResource BudgetCardStyle}"
                Background="#E8F5E9"
                BorderBrush="#C8E6C9"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="TOTAL BUDGET SPENT"
                               Foreground="#2E7D32"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="0,0,0,5" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding TotalSpent, StringFormat={}{0:N0}}"
                                   FontSize="32"
                                   FontWeight="Bold"
                                   Foreground="#1B5E20" />
                        <TextBlock Text="{Binding TotalBudgetLimit, StringFormat=/ {0:N0}}"
                                   FontSize="18"
                                   Foreground="#666"
                                   VerticalAlignment="Bottom"
                                   Margin="10,0,0,5" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            VerticalAlignment="Center">
                    <TextBlock Text="{Binding OverallProgress, StringFormat={}{0:F1}%}"
                               HorizontalAlignment="Right"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#2E7D32" />
                    <TextBlock Text="Used"
                               Foreground="#757575"
                               FontSize="12"
                               HorizontalAlignment="Right" />
                </StackPanel>
            </Grid>
        </Border>
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      Margin="0,10,0,0">
            <ItemsControl ItemsSource="{Binding BudgetList}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2"
                                     VerticalAlignment="Top" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource BudgetCardStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <DockPanel Grid.Row="0"
                                           Margin="0,0,0,15">
                                    <TextBlock Text="{Binding CategoryName}"
                                               FontSize="18"
                                               FontWeight="Bold"
                                               Foreground="#333" />
                                    <TextBlock Text="{Binding StatusText}"
                                               HorizontalAlignment="Right"
                                               FontSize="12"
                                               VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground"
                                                        Value="#757575" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding UsagePercentage, Converter={StaticResource GreaterThanConverter}, ConverterParameter=100}"
                                                                 Value="True">
                                                        <Setter Property="Foreground"
                                                                Value="#D32F2F" />
                                                        <Setter Property="FontWeight"
                                                                Value="Bold" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DockPanel>

                                <Grid Grid.Row="1"
                                      Margin="0,0,0,10">
                                    <TextBlock Text="{Binding SpentAmount, StringFormat={}{0:N0}}"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="#1B5E20" />
                                    <TextBlock Text="{Binding LimitAmount, StringFormat={}{0:N0}}"
                                               HorizontalAlignment="Right"
                                               FontSize="14"
                                               Foreground="#999" />
                                </Grid>

                                <Border Grid.Row="2"
                                        Height="12"
                                        Background="#F5F5F5"
                                        CornerRadius="6"
                                        MinWidth="200">
                                    <Grid HorizontalAlignment="Left">
                                        <Border Background="{Binding ProgressColor}"
                                                CornerRadius="6"
                                                Width="{Binding UsagePercentage, Converter={StaticResource PercentToWidthConverter}, ConverterParameter=200}"
                                                HorizontalAlignment="Left" />
                                    </Grid>
                                </Border>

                                <TextBlock Grid.Row="3"
                                           Text="{Binding UsagePercentage, StringFormat={}{0:F0}%}"
                                           HorizontalAlignment="Right"
                                           Margin="0,5,0,0"
                                           Foreground="#757575"
                                           FontSize="12" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
